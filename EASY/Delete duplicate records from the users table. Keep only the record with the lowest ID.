DELETE u1 FROM users u1
JOIN users u2 
WHERE u1.id > u2.id AND u1.email = u2.email;
OR
DELETE FROM users 
WHERE id NOT IN (
    SELECT min_id FROM (
        SELECT MIN(id) as min_id 
        FROM users 
        GROUP BY email
    ) t
);
