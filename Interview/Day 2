Find employes whose salary is greater than manager
Select e.emp_id,e.emp_name, m.emp_name as manager_name, e.salary, m.salary as manager_salary from interview.employees e 
inner join interview.employees m on e.manager_id = m.emp_id
where e.salary > m.salary;
